.env
venv/
__pycache__/
*.pyc
.SaaS
.vercel

# Staging area for raw reference uploads — only the curated per-vertical
# packs (with manifest.json) belong in the repo. _unsorted holds the raw
# pre-conversion imports.
app/services/niche_assets/**/_unsorted/
